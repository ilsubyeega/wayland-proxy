# FIXME
- [ ] verify whether the app_id can be set after initial setup
- [ ] do security audit
- [ ] rename to `prefix:` instead of `prefix` when initial state
- [ ] investigate whether the other protocol could be used for shits
- [ ] do fuzz test in order to not screw up entire environment